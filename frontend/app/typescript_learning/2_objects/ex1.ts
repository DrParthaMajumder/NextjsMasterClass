/* 
  Example 1: Basic object with fixed properties
  An object groups related data as key–value pairs.
  Each key (like 'name' or 'age') has a value, so we can access data as x_person.name, x_person.age, etc.
*/

const x_person = {
  name: "Partha",
  age: 30,
};

console.log(x_person.name); // Output: Partha
console.log(x_person.age); // Output: 30
