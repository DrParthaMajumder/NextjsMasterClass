/*
  Example 3: Tuple (fixed-length collection with specific types)
  Demonstrates:
  - Tuple syntax: [type1, type2, ...]
  - Fixed-length collections with type safety
  - Order matters in tuples
  - Different from arrays - each position has specific type
*/

const test_person: [string, number] = ["Partha", 26];
console.log("Tuple:", test_person);
